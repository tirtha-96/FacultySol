import { AlertTriangle, ArrowRight, CheckCircle2, Info } from 'lucide-react';
import type { Recommendation } from '@assessai/shared';
export function RecommendationCard({item,index,onWhy,onReview}:{item:Recommendation;index:number;onWhy:(r:Recommendation)=>void;onReview:(r:Recommendation)=>void}){const Icon=item.severity==='high'?AlertTriangle:item.severity==='medium'?Info:CheckCircle2;return <article className="rec-card"><div className={`severity-icon ${item.severity}`}><Icon size={19}/></div><div className="rec-body"><div className="rec-meta"><span>Priority {index+1}</span><span className={`pill ${item.severity}`}>{item.severity}</span></div><h3>{item.title}</h3><p>{item.reason}</p><div className="rec-actions"><button className="text-button" onClick={()=>onWhy(item)}>Why this matters</button><button className="small-button" onClick={()=>onReview(item)}>Review <ArrowRight size={15}/></button></div></div></article>}
