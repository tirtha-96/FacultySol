import { X } from 'lucide-react';import type { Recommendation } from '@assessai/shared';
export function EvidenceDrawer({item,onClose}:{item?:Recommendation;onClose:()=>void}){if(!item)return null;return <div className="drawer-backdrop" onClick={onClose}><aside className="drawer" role="dialog" aria-modal="true" aria-label="Recommendation evidence" onClick={e=>e.stopPropagation()}><button className="icon-button close" aria-label="Close" onClick={onClose}><X/></button><span className={`pill ${item.severity}`}>{item.severity} priority</span><h2>{item.title}</h2><section><label>Issue</label><p>{item.reason}</p></section><section><label>Evidence</label><p>{item.evidence}</p></section><section><label>Potential impact</label><p>{item.impact}</p></section><section><label>Suggested action</label><p>{item.action}</p></section><div className="human-note"><strong>Faculty remains in control</strong><span>This is an AI-assisted insight, not an official academic decision.</span></div><button className="primary full" onClick={onClose}>Review recommendation</button></aside></div>}
