import { BarChart3, BookOpen, FileCheck2, LayoutDashboard, LogOut, Settings } from 'lucide-react';
import { Brand } from './Brand';
export function Sidebar({active,onNavigate}:{active:string;onNavigate:(x:string)=>void}){const items=[['Dashboard',LayoutDashboard],['Courses',BookOpen],['Assessments',FileCheck2],['Reports',BarChart3],['Settings',Settings]] as const;return <aside className="sidebar"><Brand light/><nav aria-label="Primary navigation">{items.map(([x,Icon])=><button key={x} className={active===x?'active':''} onClick={()=>onNavigate(x)}><Icon size={18}/><span>{x}</span>{x==='Assessments'&&<b>3</b>}</button>)}</nav><div className="sidebar-foot"><div className="avatar">FM</div><div><strong>Dr. Farhana M.</strong><span>Faculty member</span></div><LogOut size={17}/></div></aside>}
